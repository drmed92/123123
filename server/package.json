{
  "name": "eremote-server",
  "version": "1.0.0",
  "private": true,
  "description": "ERemote central server: MQTT broker + claim API + per-device web control pages",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "aedes": "^0.51.3",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "mqtt": "^5.10.1"
  }
}
